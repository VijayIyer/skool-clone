describe("signUpVertificationForm", () => {
  it("should render all the fields as required", NotImplemented);
  it("should display the email Id that was used for signup", NotImplemented);
  it(
    "should again call request to resend email verification when user clicks on `Resend it` link",
    NotImplemented
  );
  it(
    "should go back to displaying signUpForm and no longer render signUpVerificationForm when user clicks `Use a different email` link",
    NotImplemented
  );
});
