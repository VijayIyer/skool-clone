describe("isTokenIssuedBeforePasswordChange", () => {
  it("should return true if token is issued before password changed at value", () => {
    throw Error("Test not implemented!");
  });
  it("should return false if token is issued before password changed at value", () => {
    throw Error("Test not implemented!");
  });
  it("should return false if passed in arguments are not timestamp strings", () => {
    throw Error("Test not implemented!");
  });
});
